#!/usr/bin/env ruby
%w{rubygems platform net/http}.each {|library| require library}

if :macosx != Platform::IMPL
  abort "Utility Belt's command-line Google shortcut only works on OS X. Sorry."
else
  require "#{File.dirname(__FILE__) + '/../lib'}/google"
end

google(ARGV[0])
